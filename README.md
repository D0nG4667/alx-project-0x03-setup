# alx-project-0x03-setup
In modern web applications, multiple pages often share common UI elements such as a Header and Footer. While duplicating these components across pages works, it quickly violates the DRY (Don’t Repeat Yourself) principle and leads to unnecessary redundancy. 
